{
  "opencollection": "1.0.0",
  "info": {
    "name": "Deliverart Addresses Delivery fee API",
    "version": "1.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "resource_owner_password_credentials",
      "accessTokenUrl": "https://auth.deliverart.it/oauth",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Delivery fee",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Delivery fee — Detail",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://pubapi.deliverart.it/delivery/fee/:id/detail",
            "params": [
              {
                "name": "id",
                "value": "",
                "type": "path"
              }
            ],
            "auth": {
              "type": "oauth2",
              "flow": "resource_owner_password_credentials",
              "accessTokenUrl": "https://auth.deliverart.it/oauth",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Delivery fee — Detail. Deliverart RPC-style endpoint (GET only)."
        },
        {
          "info": {
            "name": "Delivery fee — Create",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://pubapi.deliverart.it/delivery/fee/create",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "oauth2",
              "flow": "resource_owner_password_credentials",
              "accessTokenUrl": "https://auth.deliverart.it/oauth",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Delivery fee — Create. Deliverart RPC-style endpoint (POST only)."
        },
        {
          "info": {
            "name": "Delivery fee — Delete",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://pubapi.deliverart.it/delivery/fee/:id/delete",
            "params": [
              {
                "name": "id",
                "value": "",
                "type": "path"
              }
            ],
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "oauth2",
              "flow": "resource_owner_password_credentials",
              "accessTokenUrl": "https://auth.deliverart.it/oauth",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Delivery fee — Delete. Deliverart RPC-style endpoint (POST only)."
        }
      ]
    }
  ],
  "bundled": true
}